import Model from '../interface/Model';

const homeNavigations: Model[] = [
    {
        label: 'Home',
        items: [{ label: 'Dashboard', icon: 'pi pi-fw pi-home', to: '/' }]
    }
];

export default homeNavigations;
